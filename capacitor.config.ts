import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.retroghost.app',
  appName: 'Sistema Fantasma',
  webDir: 'dist/public'
};

export default config;
